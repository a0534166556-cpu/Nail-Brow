# Railway — שרת API (האתר הסטטי ב-Netlify)
[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "node server/index.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
